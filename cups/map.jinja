
{%- load_yaml as base_defaults %}

Debian:
  pkgs:
  - cups
  - cups-filters
  - cups-browsed
  - avahi-daemon

{%- endload %}

{%- set service = salt['grains.filter_by'](base_defaults, merge=salt['pillar.get']('cups:service')) %}
